name: Auto Semantic Index Refresh (Disabled)

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest

    steps:
      - name: Workflow disabled
        run: echo "auto_index.yml is intentionally disabled and performs no actions."
