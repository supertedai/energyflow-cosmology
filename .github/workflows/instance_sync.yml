name: Instance Sync (Disabled)

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "instance_sync.yml disabled to prevent auto-regeneration."
