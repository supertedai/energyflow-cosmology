name: Debug Secrets
on: workflow_dispatch

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Show if secret exists
        run: |
          if [ -z "${{ secrets.GCP_CREDENTIALS_JSON }}" ]; then
            echo "SECRET_EMPTY"
          else
            echo "SECRET_PRESENT"
          fi
