name: Auto Update Sitemap Schema

on:
  push:
    branches:
      - main
  schedule:
    - cron: "0 3 * * *"  # kjører hver natt kl 03:00 UTC
  workflow_dispatch:

jobs:
  update-schema:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: 🧩 Hent repository
        uses: actions/checkout@v4

      - name: 🛠️ Installer jq og curl
        run: sudo apt-get update && sudo apt-get install -y jq curl

      - name: 🔄 Kjør update-schema.sh
        run: bash schema/update-schema.sh

      - name: 💾 Commit og push endringer
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "Morten Magnusson"
          git config user.email "morten@magnusson.as"
          git add schema/sitemap-links.json

          if git diff --cached --quiet; then
            echo "ℹ️ Ingen endringer å pushe."
          else
            git commit -m "🤖 Auto-update sitemap-links.json [GitHub Actions]"
            git push origin main
            echo "🚀 Oppdatering fullført og pushet til main."
          fi
