name: Repo Metadata (Disabled)

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "repo_metadata.yml disabled to prevent auto-metadata generation."
